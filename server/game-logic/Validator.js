import{RankSystem}from'./RankSystem.js';export class Validator{static getPlayType(c,o){if(!c||c.length===0)return{type:'invalid',error:'No cards'};const s=RankSystem.sortCards(c,o);if(c.length===1)return{type:'single',cards:s,rank:RankSystem.rankValue(s[0],o),length:1};const r=[...new Set(c.map(x=>x.rank))];if(r.length===1)return{type:'set',cards:s,rank:RankSystem.rankValue(s[0],o),length:c.length};return{type:'invalid',error:'Invalid'};}static canBeatPlay(n,l,o){if(!l||l.type==='none')return{canBeat:true};if(n.type!==l.type||n.length!==l.length)return{canBeat:false};return n.rank>l.rank?{canBeat:true}:{canBeat:false};}}