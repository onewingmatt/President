export class Card { constructor(rank, suit) { this.rank = rank; this.suit = suit.toUpperCase(); } toJSON() { return { rank: this.rank, suit: this.suit }; } toString() { const suitSymbols = { 'H': '♥', 'D': '♦', 'C': '♣', 'S': '♠' }; return this.rank + (suitSymbols[this.suit] || this.suit); } }